# TODO

## Phase 1: Content expansion plan (approved)
- [x] Inspect repo structure and key wrapper components (`PageShell`, CSS).
- [x] Read all existing page components in `frontend/src/pages`.

## Phase 2: Implement “big pages” consistently
- [x] Update `Home.jsx` with extra sections (FAQ, detailed service/process blocks) while avoiding new unverifiable stats.

- [ ] Update `About.jsx` with additional content blocks (vision-mission expansion, service/process/FAQ, CTAs).

- [ ] Update `About-new.jsx` similarly.

- [x] Update `Operations.jsx` to add deeper explanation sections + FAQ.
- [x] Update `EServices.jsx` to add process + FAQ.


- [ ] Update `Auctions.jsx`, `Tenders.jsx` with detailed sections.




- [ ] Update `Downloads.jsx` with additional guidance + FAQ.

- [ ] Update `Directory.jsx` with explanation + guidance.
- [ ] Update `ForestryActivities.jsx` with extra blocks.
- [ ] Update `Projects.jsx` with extra blocks.
- [ ] Update `Services.jsx` with expanded explanation.
- [ ] Update `TurnkeyProjects.jsx` with extra sections.
- [ ] Update `Gallery.jsx` with additional captioned content blocks.

## Phase 3: Validate
- [ ] Run frontend lint/build to ensure no JSX errors.


